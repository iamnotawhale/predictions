# Профиль prod: spring.profiles.active=prod (см. deploy/predicts.env)
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/predicts_prod
    username: admin

miniapp:
  dev-mode: false

# На проде не пересчитывать 60+ матчей при каждом рестарте (~40 с CPU/RAM)
predictions:
  startup:
    recalculate: false
    sync-match-times: true

logging:
  level:
    org:
      springframework:
        security: WARN
