# Локальный профиль: --spring.profiles.active=local (scripts/run-local.sh)
# Переопределения БД и секретов: deploy/local.env (см. deploy/local.env.example)
spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/predicts_local}
    username: ${SPRING_DATASOURCE_USERNAME:admin}
    password: ${SPRING_DATASOURCE_PASSWORD:postgres}

bot:
  username: eplinfodevbot
  token: 6610261457:AAHofB9PStvQBH9NTKzddjy7abxMQOtOo40
  chatId: -4551040804

miniapp:
  dev-mode: true
  dev-telegram-id: "739299"

predictions:
  startup:
    # false — не гонять пересчёт по продовой БД при каждом локальном старте
    recalculate: false
    sync-match-times: false

api:
  football:
    token: 8b72220a5a43c9b249b85d81652f7aef

# HTTPS для Telegram Mini App: ./scripts/dev-https.sh → deploy/local-https.env
