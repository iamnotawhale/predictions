# Продакшен: cp deploy/predicts.env.example deploy/predicts.env && заполните секреты
# Файл deploy/predicts.env в .gitignore

SPRING_PROFILES_ACTIVE=prod

# Telegram (@eplinfobot)
BOT_USERNAME=eplinfobot
BOT_TOKEN=
BOT_CHAT_ID=-1001762896029

# Админ для panic-уведомлений (личный chat id)
ADMIN_CHAT_ID=739299

# БД (пароль postgres на сервере; url задаётся в application-prod.yml)
SPRING_DATASOURCE_PASSWORD=postgres

# API-Football
API_FOOTBALL_TOKEN=

# Mini App HTTPS: bash deploy/setup-caddy-sslip.sh (рекомендуется, порт 8443)
# trycloudflare ломается с ошибкой 1033, когда cloudflared остановлен.
# PUBLIC_DOMAIN=146-255-188-80.sslip.io
# PUBLIC_HTTPS_URL=https://146-255-188-80.sslip.io:8443
# BOT_WEBAPP_URL=https://146-255-188-80.sslip.io:8443/miniapp/
APP_PORT=8080
