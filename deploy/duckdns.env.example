# cp deploy/duckdns.env.example deploy/duckdns.env && заполните (файл в .gitignore)
# Читает predictions-net-refresh на проде (не Spring)

DUCKDNS_DOMAIN=your-subdomain
DUCKDNS_TOKEN=
DUCKDNS_FULL=your-subdomain.duckdns.org
PUBLIC_IP=
HTTPS_PORT=443
