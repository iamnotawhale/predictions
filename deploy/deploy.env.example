# cp deploy/deploy.env.example deploy/deploy.env && заполните (файл в .gitignore)
# source deploy/deploy.env перед ./deploy/upload-jar.sh

DEPLOY_SERVER=root@your-host
DEPLOY_APP_DIR=/home/predictions
