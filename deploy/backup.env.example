# cp deploy/backup.env.example deploy/backup.env  (gitignore)
# Читает backup-to-vps.sh на Odyssey

VPS_SSH=root@YOUR.VPS.IP
VPS_APP_DIR=/home/predictions
VPS_BACKUP_DIR=/var/backups/predictions
KEEP_DUMPS=8
